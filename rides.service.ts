export async function estimate(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'estimate',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function request(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'request',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function accept(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'accept',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function start(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'start',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function complete(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'complete',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function cancel(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'cancel',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function rate(body: any, params?: any, query?: any) {
  return {
    module: 'rides',
    action: 'rate',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}
