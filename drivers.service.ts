export async function apply(body:any, params?:any, query?:any) {
  return {
    module: 'drivers',
    action: 'apply',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function availability(body:any, params?:any, query?:any) {
  return {
    module: 'drivers',
    action: 'availability',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function location(body:any, params?:any, query?:any) {
  return {
    module: 'drivers',
    action: 'location',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function earnings(body:any, params?:any, query?:any) {
  return {
    module: 'drivers',
    action: 'earnings',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}

export async function documents(body:any, params?:any, query?:any) {
  return {
    module: 'drivers',
    action: 'documents',
    status: 'TODO_CONNECT_REAL_SERVICE',
    body,
    params,
    query
  };
}
