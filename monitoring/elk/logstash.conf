input {
  beats {
    port => 5044
  }
}

filter {
  if [fields][service] {
    mutate { add_field => { "service" => "%{[fields][service]}" } }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "drive-logs-%{+YYYY.MM.dd}"
  }
}
